# Foreclosure Ingestion Pipeline

Scrapes and parses foreclosure listings from Texas county courthouse sources.

## Components

- `scraper.py` — HTTP scraper for county foreclosure pages
- `parser.py` — Normalizes raw data into structured property events

## Data Sources

> TODO: Document per-county source URLs and schedules.

## Output Schema

> TODO: Link to `property_event` schema once finalized.
